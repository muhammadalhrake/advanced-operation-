function secondGeeneratS(
  firstNumber: number[],
  level: string,
  secondDigits: number
) {
  if (level == 'easy') {
    for (let i = 0; i < secondDigits; i++) {
      if ('first poss') {

      } else {

      }
    }
  } else {
    if ('first poss') {
      
    }
  }
}
